output/

**.bak
*.local.*
secrets.local.ps1
!**/README.md
.kitchen/

*.nupkg
*.suo
*.user
*.coverage
.vs
.psproj
.sln
markdownissues.txt
node_modules
package-lock.json
.DS_Store

# Editor / agent tooling artifacts
.omo/
PowerShellEditorServices.json

# graphify skill output
graphify-out/

# Local secrets
.env
.env.local
.env.*.local

# Local AI-agent tooling (Claude Code / Ruflo / claude-flow) — never ship this
# scaffolding into modules created from this template. It carries auto-executing
# hooks and has had real path-traversal/dynamic-import bugs in this repo's history
# (see commits 70fe9a3, 30f70a3) — keep it local and untracked, not committed.
.claude/
.claude-flow/
.agents/
.swarm/
ruvector.db
