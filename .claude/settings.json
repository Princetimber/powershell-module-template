{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -Command \"$files = git diff --name-only --diff-filter=AM -- '*.ps1' '*.psm1'; if ($files) { Invoke-ScriptAnalyzer -Path $files -Settings PSScriptAnalyzerSettings.psd1 -Severity Warning,Error }\""
          }
        ]
      }
    ]
  }
}
