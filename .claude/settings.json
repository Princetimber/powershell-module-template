{
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "pwsh -Command \"$files = git diff --name-only --diff-filter=AM -- '*.ps1' '*.psm1'; if ($files) { Invoke-ScriptAnalyzer -Path $files -Severity Warning,Error }\""
      }
    ]
  }
}
